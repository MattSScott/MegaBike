## Running code

```bash
# Approach 1
go run . # Linux and macOS: Use `sudo go run .` if you encounter any "Permission denied" errors.

# Approach 2
go build # build step
./MegabikeFYPVersion # MegabikeFYPVersion.exe if you're on Windows. Use `sudo` on Linux and macOS as Approach 1 if required.
```